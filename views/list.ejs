<%- include('header'); -%>
    <div class="box" id="heading">
      <h1>
        <%= listTitle %>
      </h1>
    </div>
    <div class="box">
      <% for (let i=0; i < newListItem.length; i++){ %>
        <div class="item">
          <input type="checkbox">
          <p>
            <%= newListItem[i] %>
          </p>
        </div>

        <% } %>
          <form action="/" method="post" class="item">
            <input type="text" name="newItem" id="newItem" placeholder="New item" autocapitalize="off">
            <button type="submit"  name="list">+</button>
          </form>
    </div>
  <!-- Bootstrap JavaScript Libraries -->
  <%- include('footer'); -%>